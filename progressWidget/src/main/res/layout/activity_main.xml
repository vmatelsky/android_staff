<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Push me!"/>

    <FrameLayout
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:clipChildren="false"
        android:layout_centerInParent="true">

        <com.vlabs.progresswidget.ProgressWidget
            android:id="@+id/second_widget"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:stroke_color="@color/second_stroke_color"
            app:stroke_width="@dimen/second_stroke_width"/>

        <com.vlabs.progresswidget.ProgressWidget
            android:id="@+id/progress_widget"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:stroke_color="@color/stroke_color"
            app:stroke_width="@dimen/stroke_width"/>

    </FrameLayout>

</RelativeLayout>
