<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.vlabs.androiweartest"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:name=".WearApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".activity.launch.WearMainActivity"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="MyStubBroadcastActivity"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.pick.PickStationActivity"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="MyStubBroadcastActivity"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault.Light" />

        <activity
            android:name=".activity.notification.NotificationActivity"
            android:allowEmbedded="true"
            android:exported="true"
            android:label="MyStubBroadcastActivity"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault.Light" />

        <service
            android:name=".WearListenerService">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.BIND_LISTENER" />
            </intent-filter>
        </service>

        <meta-data android:name="com.google.android.gms.version"
                   android:value="@integer/google_play_services_version" />
    </application>

</manifest>
